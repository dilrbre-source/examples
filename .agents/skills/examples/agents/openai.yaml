interface:
  display_name: "Examples"
  short_description: "Repo-specific patterns and workflows for examples"
  default_prompt: "Use the examples repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true